## What this changes

<!-- And why. -->

Closes #

## Checklist

- [ ] `mkdocs build --strict` passes — it fails on a broken link or a page
      missing from the nav
- [ ] A new page has an entry in `nav:` in `mkdocs.yml`
- [ ] Nothing from `internal/` moved into `docs/` without being read first
- [ ] If this documents behaviour, it matches what the code actually does
